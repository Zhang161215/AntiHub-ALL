services:
  backend:
    image: antihub-backend:local
    build:
      context: ./AntiHub-Backend
      dockerfile: Dockerfile

  plugin:
    image: antihub-plugin:local
    build:
      context: ./AntiHub-plugin
      dockerfile: Dockerfile

  web:
    image: antihub-web:local
    build:
      context: ./AntiHub
      dockerfile: Dockerfile
      additional_contexts:
        antihook: ./AntiHook
